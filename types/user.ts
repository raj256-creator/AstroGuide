export interface User {
  id: string;
  email: string;
  name: string;
  dateOfBirth: string;
  zodiacSign: string;
  createdAt: string;
}

export interface UserProfile {
  name: string;
  email: string;
  dateOfBirth: string;
  zodiacSign: string;
}